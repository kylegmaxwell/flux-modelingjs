Tests can be run using './run_tests.sh' command from the current directory.
Tests are using 'jasmine-node' package, that will be install locally if needed.
If 'nodejs' or 'npm' is not installed, script will try to install them (requires root password).
